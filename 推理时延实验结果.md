# 多模态预测模型推理时延实验

## 测量设置

测试硬件为 NVIDIA GeForce RTX 4090，软件环境为 PyTorch 2.8.0 和 CUDA 12.8。模型加载测试性能对应的最佳 checkpoint，采用 `eval`、`inference_mode` 和 FP16 autocast，batch size 为 1。真实测试样本在计时前已经载入 GPU，因此结果只包含模型前向计算，不包含 HDF5 读取、JPEG 解码、CPU 到 GPU 的数据传输和网络传输。

每个模型预热 50 次，再使用 CUDA Event 测量 300 次完整前向；分阶段时延独立测量 200 次。下表采用中位数，完整结果还保存了均值和 P95。全部模型独立复测一轮，两轮中位数的最大相对偏差为 1.69%。

## 端到端单次推理时延

| 任务 | A | B | C | D | E | F | G |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| CSI（ms） | 5.79 | 6.74 | 6.73 | 10.30 | 9.31 | 10.15 | 7.42 |
| 波束（ms） | 2.47 | 3.41 | 3.42 | 7.23 | 6.30 | 7.21 | 6.48 |

## 分阶段中位时延

无线分支包含无线特征提取与模态内时空建模；运动分支包含傅里叶特征映射、多层感知机和模态内时空建模；RGB 分支包含 ConvNeXt-Tiny 和视觉模态内时空建模。数值单位均为 ms。

| 任务 | 组合 | 无线分支 | 运动分支 | RGB 分支 | 跨模态融合 | 未来解码 | 输出头 |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: |
| CSI | A | 2.979 | — | — | 1.005 | 0.891 | 1.011 |
| CSI | B | 2.978 | 1.029 | — | 0.962 | 0.881 | 1.009 |
| CSI | C | 2.970 | 1.037 | — | 0.961 | 0.884 | 1.007 |
| CSI | D | 2.921 | 1.021 | 3.764 | 0.958 | 0.873 | 1.000 |
| CSI | E | 2.900 | — | 3.727 | 0.962 | 0.857 | 0.985 |
| CSI | F | 2.894 | 1.012 | 3.733 | 0.951 | 0.862 | 0.986 |
| CSI | G | — | 1.062 | 3.716 | 0.950 | 0.860 | 0.978 |
| 波束 | A | 0.774 | — | — | 0.905 | 0.811 | 0.082 |
| 波束 | B | 0.791 | 0.928 | — | 0.904 | 0.822 | 0.083 |
| 波束 | C | 0.795 | 0.945 | — | 0.905 | 0.824 | 0.083 |
| 波束 | D | 0.814 | 0.956 | 3.735 | 0.945 | 0.849 | 0.085 |
| 波束 | E | 0.809 | — | 3.696 | 0.937 | 0.848 | 0.085 |
| 波束 | F | 0.814 | 0.968 | 3.735 | 0.944 | 0.852 | 0.086 |
| 波束 | G | — | 0.982 | 3.708 | 0.940 | 0.855 | 0.085 |

分阶段测试需要在边界插入额外 CUDA Event，因此分项中位数之和与单独测量的端到端中位数会存在少量差异。

## 主要发现

RGB 编码约需 3.7 ms，是附加模态中最主要的计算开销；位置或完整运动状态分支约需 1.0 ms。共享的跨模态融合和未来状态解码合计约为 1.8 ms。CSI 的无线分支约需 3.0 ms，明显高于波束任务的约 0.8 ms，这是因为 CSI 分支需要同时进行天线—频率域和角度—时延域编码。CSI 的二维重建输出头约需 1.0 ms，而 64 类波束输出头只需约 0.08 ms。

结合预测精度，CSI 组合 B 相比 A 的中位时延增加 0.95 ms，NMSE 从 −7.91 dB 改善到 −9.57 dB；D 获得最佳的 −10.10 dB，但中位时延增加到 10.30 ms。位置在当前场景中具有较好的精度—时延折中，RGB 则以更高开销换取进一步增益。

波束任务中，A 同时获得最高 Top-1 和最低时延。F 的 Top-3 相比 A 只提高 0.28 个百分点，中位时延却由 2.47 ms 增至 7.21 ms。当前短期预测条件下，持续启用视觉与完整运动分支缺少明显的收益依据；它们在递归预测后续步中的作用更适合按运行状态动态启用。

最慢的 CSI 与波束组合分别为 10.30 ms 和 7.23 ms，均低于当前任务的采样间隔。不过，这只说明模型前向计算满足当前周期，不能替代包含传感器采集、同步、传输和调度的端到端系统测量。

## 可复现实验文件

服务器目录 `/root/autodl-tmp/simart-m2stformer/experiments/latency/` 包含：

- `benchmark_latency.py`：正式测量脚本；
- `results/latency_results.json`：环境、测量协议和完整统计；
- `results/latency_summary.csv`：全部模型的汇总数据；
- `LATENCY_RESULTS_ZH.md`：更完整的中文技术说明。
